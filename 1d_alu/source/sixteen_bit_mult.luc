/** 
mult and div
00 0010: MUL
00 0011: DIV
*/
module sixteen_bit_mult (
    input alufn,
    input x[16],
    input y[16],
    output out[16]
  ) {

  always {
  case(alufn) {
      b0: out = x*y;
      b1: out = x/y;
      default: out = 16b0;
    }
  }
}
